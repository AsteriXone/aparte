<nav class="menubar navbar navbar-inverse navbar-fixed-top">
    <!--div class="container-fluid">
        <img style="max-height: 50px" class="img-responsive center-block" src="img/aparteLogo.jpg"/>
    </div-->
    <div class="container-fluid">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <span id="header_shadow"></span>
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="{{ url('homepage') }}"> <img style="max-height: 50px" src="{{ asset('img/aparteLogo.jpg') }}"/></a>
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav">
                {% if app.user %}
                    <!-- Menú para usuarios registrados -->
                    <li><a href="#">Mi Pedido</a></li>
                    <li><a href="#">Votar Orla</a></li>
                    <li><a href="#">Votar Profe</a></li>
                    <li><a href="#">Pedir Cita</a></li>
                    <li><a href="#">Contactar</a></li>
                {% else %}
                    <li><a href="{{ path('orlas') }}">Orlas</a></li>
                    <li><a href="{{ path('mantenimiento') }}">Fotoescuela</a></li>
                    <li><a href="{{ path('mantenimiento') }}">Graduaciones</a></li>
                    <li><a href="{{ path('mantenimiento') }}">Retratos</a></li>
                    <li><a href="{{ path('mantenimiento') }}">Reportajes</a></li>
                    <li><a href="{{ path('mantenimiento') }}">Quienes Somos</a></li>
                    <li><a href="{{ path('mantenimiento') }}">Contactar</a></li>
                {% endif %}
            </ul>
            <ul class="nav navbar-nav navbar-right">
                 <li>
                     {% if app.user %}
                         <a href="{{ path('fos_user_security_logout') }}">
                             Cerrar Sesión
                             <span class="glyphicon glyphicon-log-out"></span>
                         </a>
                     {% else %}
                         <a href="{{ path('fos_user_security_login') }}">
                             Acceso
                             <span class="glyphicon glyphicon-user"></span>
                         </a>
                     {% endif %}
                </li>
            </ul>
        </div><!-- /.navbar-collapse -->
    </div><!-- /.container-fluid -->
</nav>